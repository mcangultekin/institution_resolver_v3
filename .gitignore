# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/
.pytest_cache/

# OS / IDE
.DS_Store
.idea/

# Veri: ham + islenmis buyuk dosyalar versiyonlanmaz (raporla yeniden uretilir)
data/raw/
data/processed/*.csv
data/processed/*.jsonl
data/processed/embeddings*
# transform_report.json versiyonlanir (kucuk + korpus profili kaniti)
!data/processed/transform_report.json

# Gercek affiliation ornekleri (kisisel veri) - ASLA commit edilmez
data/inbox/
data/eval/*_labeled.csv
data/eval/*_sample.csv
# ...ve bu sorgulari satir satir tasiyan analiz ciktilari (ayni kisisel veri)
data/eval/ab_*.csv
data/eval/gate_batch_*.csv
data/eval/decide_batch_*.csv
data/eval/gold_kuyruk_*.csv

# API batch job girdi/ciktilari (kullanici verisi, bkz. api/routers/batch.py)
data/jobs/

# Kosu ciktilari: batch/probe CSV'leri + loglar. Bunlar da sorgu metnini satir
# satir tasiyor (ayni kisisel veri) - deney sonuclari docs/RAPOR_*.md'de ozetlenir.
output/
# CLI varsayilan cikti adlari repo koküne duser (bkz. cli/main.py --out)
/batch_sonuc.csv
/gate_batch_sonuc.csv
/decide_batch_sonuc.csv
*_sonuc.csv

# LLM cache + sirlar
.env
*.cache
llm_cache/

# R
.Rhistory

# Uretim envanteri: hangi kaydin (yazar/hakem/kullanici) hangi kuruma bagli
# oldugunu satir satir tasiyor - gercek kisisel/kurumsal veri, versiyonlanmaz.
# Normalize edilmis turevi de ayni veriyi tasir.
institution-field-inventory*.csv

# Gercek sirket sorgulari - repo PUBLIC, hicbiri commit edilmez
data/eval/
faz0_sonuc/
# Katalogdan ~3 sn'de uretilen tureviler (kaynak JSONL zaten ignore'lu)
data/processed/token_df.json
data/processed/query_token_df.json
# Ekran goruntuleri
*.png
