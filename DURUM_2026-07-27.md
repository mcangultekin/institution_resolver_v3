# F4 Hakem Katmanı — Durum Notu (2026-07-27)

> Bu dosya, kredi bitmeden önce oturumun durumunu kaydetmek için yazıldı.
> Önceki oturum Fable 5 ile çalıştı; kod değişikliklerinin hepsi **lokal, commit'siz**.

---

## 1. Ne yapıyoruz

F4 hakem katmanı (`judge/`): `resolve()`'un bulduğu aday havuzundan (kurum =
parent + alt-birim = subunit) doğru kaydı LLM ile seçiyoruz. Model: yerel Gemma,
Ollama üzerinden. Claude/Anthropic F4'te KULLANILMIYOR (maliyet — canlıya
alınacak).

---

## 2. Bulunan kök neden (KANITLANDI)

**Ollama sessiz prompt kırpması.** `num_ctx` gönderilmediği için Ollama prompt'u
~2048 token'da sessizce kesiyordu. 18 adaylı prompt ~4600 token; model sorguyu,
hipotezleri ve listenin başını (1. sıradaki doğru aday dahil) HİÇ görmüyordu.
"Ege / Geriatri" örneğindeki iki saçma hatanın da açıklaması buydu.

**Düzeltme:** `judge/client.py` içinde `num_ctx=8192` açıkça gönderiliyor +
kırpılma olursa (`prompt_eval_count >= num_ctx`) artık sessiz geçmeyip `LlmError`
fırlatıyor.

---

## 3. Yapılan yapısal düzeltmeler (hepsi lokal)

Değişen dosyalar (`git status`'ta M olarak görünürler):
- `src/institution_resolver_v3/judge/candidates.py`
- `src/institution_resolver_v3/judge/client.py`
- `src/institution_resolver_v3/judge/judge.py`
- `src/institution_resolver_v3/judge/prompt.py`
- `src/institution_resolver_v3/judge/schema.py`
- `src/institution_resolver_v3/retrieve/resolve.py`
- `tests/unit/test_judge.py`

### 3.1 num_ctx + kırpılma koruması (`client.py`)
Yukarıda; ayrıca `generate()` artık opsiyonel `format_schema` alıyor
(structured output).

### 3.2 Şema-kısıtlı çıktı (id halüsinasyonu yapısal olarak kapandı)
- Ollama'nın kısıtlı-üretim (structured output) özelliği kullanılıyor.
- `matched_id` yalnızca ilgili listenin adaylarından seçilebiliyor; enum değeri
  `"P1|EGE ÜNİVERSİTESİ (English alias)"` biçiminde — kısa sentetik etiket + ad
  (+ İngilizce alias). Kodda `_label_views` ile gerçek id'ye çevriliyor.
- Bu, üç hata sınıfını FİZİKSEL olarak engelledi:
  1. Liste karıştırma (parent alanına subunit id'si — "105863" vakası),
  2. Komşu id yazma (doğru adayı görüp yanındaki id'yi yazma — Gazi/Cardiology),
  3. Format hataları (matched_id unutma, verdict'e "null" yazma).
- `_validate_ids` "kemer + pantolon askısı" olarak duruyor.

### 3.3 Prompt iyileştirmeleri (`prompt.py`)
- Eşleşen parça gösterimi (`exact_match_text`),
- İngilizce alias köprüsü (`best_alias` → `diğer_adı=`),
- `unit_phrase` ara-adımı (subunit'ten ÖNCE, en spesifik birim ifadesini
  yazdırıp dikkat çapası kuruyor),
- 3-seviye kuralı (Üniversite > Fakülte > Bölüm; fakülte kendi başına hedef
  değil),
- ülke/şehir tutarlılık kuralı, "örtüşmüyorsa no_match" kuralı.

### 3.4 resolve.py sinyalleri
- `exact_match` + `exact_match_text` (İÇERME, tam eşitlik değil),
- `best_alias` (sorguya en yakın alias — TR katalog / EN sorgu köprüsü),
- tsr artık name + HER alias'a karşı hesaplanıp en iyisi alınıyor (yabancı-dil
  kaçağı düzeldi).

---

## 4. Aşama 1 (BUGÜN yapıldı — hız paketi)

1. **`query` alanı çıktıdan kaldırıldı** — model koca sorguyu geri yazmıyor
   artık. Şema (schema.py), format şeması (judge.py), prompt örneği, testler
   güncel. **144/144 test geçiyor.**
2. **Prompt cache-dostu sıraya alındı** — sabit blok (görev + notlar + kurallar
   + çıktı şeması) başta, değişken kısım (sorgu + hipotez + adaylar) sonda.

**Aşama 1 doğrulama sonuçları (25 atlamalı sorgu):**

| | E4B referans | E4B Aşama-1 | E2B Aşama-1 |
|---|---|---|---|
| auto_match | 20 | 23 | 25 |
| review/ambiguous/no_match | 3/0/1 | 0/1/0 | 0/0/0 |
| doğrulama hatası | 1 | 1 | 0 |
| ort. süre (resolve dahil) | 30,4 s | 30,1 s | 19,8 s |

- **Davranış stabil:** kurum seçimi ~%100 aynı (E2B/E4B arası bile 24/23 aynı id).
- **DİKKAT — kalibrasyon kayması:** Aşama-1 yeniden sıralaması E4B'nin temkinli
  kararlarını eritti (referansta 3 review + 1 no_match → Aşama-1'de 0 + 0, sadece
  1 ambiguous). Kurallar artık adaylardan önce geldiği için model karar anında
  onları daha az tartıyor gibi. **Çözüm önerisi (henüz yapılmadı):** aday
  listelerinden SONRA, üretimden hemen önce tek satır sabit kuyruk hatırlatması:
  *"Unutma: hiçbir aday gerçekten örtüşmüyorsa no_match; emin değilsen
  review/ambiguous."*
- **Hız:** İzole cache testi mekanizmayı kanıtladı (prompt işleme 2,93 s → 0,28 s,
  10 kat), AMA uçtan uca ortalamada görünmüyor çünkü sürenin ~%50'si resolve() ve
  gürültüsü LLM kazancını yutuyor. Bu Ollama sürümü `/api/generate` çağrıları
  arası prefix cache'i tutmuyor olabilir.

---

## 5. Model karşılaştırması özeti (E2B vs E4B)

- E4B'nin eski elenme gerekçesi (format hataları) şema-kısıtıyla ORTADAN KALKTI.
- E4B kalibrasyonu belirgin daha iyi: şüpheli vakalarda review/ambiguous/no_match
  kullanıyor; E2B neredeyse her şeye auto_match diyor.
- Hız bedeli: E4B ~%20-50 daha yavaş.
- Ortak kalan 2 kayıp (hakem sorunu DEĞİL): (a) Adli Tıp Kurumu havuza hiç
  gelmiyor — İngilizce alias eksik (veri işi); (b) UZ/RU adaşında ülke çelişkisi
  prompt'ta görünmesine rağmen iki model de yakalamıyor → **kodda ülke_çelişkisi
  bayrağı hesaplanmalı** (nitelik_çelişkisi gibi).
- E4B regresyonu: MPG şirket vakası — eski E4B no_match derken yeni koşuda "Max
  Planck Society"ye (MPG kısaltması!) auto_match verdi. `reasoning` alanı
  kaldırıldığı için "bu bir şirket" muhakemesini yazacak yeri yok.

---

## 6. Kararların güncel durumu (Oturum 2, 2026-07-27 Opus)

1. **`judge.model` → `gemma4:e4b`** → ✅ **YAPILDI.** `config/default.yaml`
   güncellendi (değer + gerekçe yorumu). Karar iki ayrı 25/50-sorgu koşusunda
   E4B kalibrasyonunun belirgin üstünlüğüne dayanıyor.
2. **ambiguous'ta `matched_id=null`'a izin (şema-fix)** → ⛔ **TEST EDİLDİ,
   REDDEDİLDİ.** Şema gevşetildi + 25 sorguda koşuldu; Başkent/Cairo hataları
   geçerli çıktıya döndü AMA net kalite kazancı çıkmadı (n=25 gürültü sınırı,
   E4B stokastik). Kullanıcı kararı: **baseline'a dönüldü** (şema Aşama-1 katı
   hâline geri alındı).
3. **Aşama 2** (kural sadeleştirme + hipotez sadeleştirme) → ⛔ **TEST EDİLDİ,
   REDDEDİLDİ.** 25 sorgu × E2B/E4B; parent'lar stabil, ~%8 hız ama kanıtlanmış
   kalite kazancı yok + kalibrasyon kayması. **Baseline'a dönüldü.**
4. **ülke_çelişkisi bayrağı** → ⏸️ ERTELENDİ (kullanıcı: "ülke çıkaran mekanizma
   ekleme"). Gazetteer gerektiriyor; şimdilik yok.
5. **decide/ katmanı, kuyruk hatırlatması, MPG çapası** → hâlâ açık (§7).

**Ertelenenler:** İngilizce talimat A/B'si, kozmetik sinyal düzenlemeleri,
Dockerize (Mac'te Ollama GPU'suz kalır → yerelde native tut, prod Linux'ta
container + nvidia; model yerel GGUF tag olduğu için `ollama pull` çalışmaz,
`~/.ollama/models` kopyalanmalı).

---

## 6b. Kosinüs sinyali — teşhis + KARAR (Oturum 2, 2026-07-27)

**Sorun:** kosinüs hep yüksek çıkıyor. **Kök neden (ÖLÇÜLDÜ):** embedding modeli
`multilingual-e5-base` **anizotropik** — tüm benzerlikler dar bir koniye sıkışıyor:
- Rastgele (alakasız) parent-parent çiftleri: **μ=+0.837, σ=0.032**, aralık
  [0.71, 0.98]. "kuru fasulye tarifi" bile üniversite sorgusuna 0.755 veriyor.
- Havuz-içi sıra testi (8 net vaka): gerçek eşleşmeyi **kosinüs 1/8 kez 1.
  sıraya** koydu (ort. sıra ~4). Buna karşılık **bm25 ve token_benzerlik 8/8.**
- Yani ham/göreli kosinüs hakem için **yanıltıcı ranking sinyali**; dağılım-tabanlı
  yeniden ölçekleme (z-skor) de kurtarmaz (sinyal üst kuyrukta değil).

**Ama kNN recall'ı kurtarıyor:** çapraz-dilli/exonym vakalarda (Bosphorus→Boğaziçi,
Aegean→Ege) bm25 tamamen kaçırırken kNN doğru kaydı ~2. sıraya çekiyor. *(Uyarı:
exonym vakalarının bir kısmı sahte gold'du — "Aegean University" aslında Yunan
"University of the Aegean"a oturuyor; kNN recall değeri gerçek ama oran abartılmamalı.)*
Akronimleri (METU/ITU) ise kNN değil leksik/alias çözüyor → **kNN + leksik tamamlayıcı.**

**KARAR (uygulandı):**
- **kNN retrieval'da KALIR** (`resolve.py` değişmedi) — çapraz-dilli recall için.
- **Hakem ham kosinüsü GÖRMEZ** — `prompt.py`'den `kosinüs=` çıkarıldı, `_fmt_cosine`
  ve "KOSİNÜS UYARISI" silindi, docstring güncellendi; `test_judge.py` yeni
  davranışı doğruluyor (`test_cosine_not_shown_to_judge`).
- Kosinüs hâlâ **hesaplanıp** `ScoredCandidate`/`CandidateView`'da taşınıyor
  (CLI/debug/gelecek için); CLI insan çıktısı değişmedi.

---

## 6c. F5 batch entegrasyonu — YAPILDI (Oturum 2, commit 533ff4c)

Sıfırdan kuruldu: bir girdi CSV'sindeki kurum ifadelerini `resolve → judge`
zincirinden geçirip **sonuç CSV'sine** yazan batch.
- **`eval/batch.py`** — `process_one` (tek sorgu, düz kayıt) + `run_batch`
  (progressive CSV yazımı = çökme-güvenli, satır-bazlı hata izolasyonu, `--limit`,
  `--resume`). `no_match/review/ambiguous` HATA DEĞİL (`status=ok`, verdict'te
  yazılır); gerçek hata (bağlantı/doğrulama) → `status=error`.
- **CLI**: `inres3 batch <input.csv> --query-col raw_name --out sonuc.csv
  [--limit N] [--resume] [--model ...]`.
- **Çıktı kolonları**: query, status, parent_verdict/id/name, subunit_verdict/id/name,
  unit_phrase, error, resolve_s, llm_s, **result_json** (tam sadakat).
- **Test**: `tests/unit/test_batch.py` 6 test (resolve/judge enjekte, ES/Ollama'sız).
  Toplam 150/150 test geçiyor.
- **Gerçek smoke**: v2 CSV'sinden (438.464 satır, `raw_name`+`canonical_id`) 3
  satır → 3/3 doğru.
- **Gate/decide bilerek ERTELENDİ** (gate eşikleri gold sonrası; decide boş).
  `process_one` tek nokta, ikisi sonradan araya girer.

**Ölçek uyarısı:** ~30-50 s/sorgu (E4B) → tam 438k İMKÂNSIZ (aylar). Batch,
alt-küme/örneklem ya da daha küçük girdi için. Ölçek istenirse **gate** (LLM'siz
kolay-vaka elemesi) şart. `judge.enabled` hâlâ `false` (batch komutu bayrağı
okumuyor, hakemi doğrudan çağırıyor).

**Süre (aynı 25 sorgu, resolve dahil):** E2B ~20 s, E4B ~30 s (E4B ~%50 yavaş;
LLM baskın maliyet ~15-34 s, resolve sıcakta ~2-6 s).

---

## 6d. Gate Aşama-1 — YAPILDI (Oturum 3, 2026-07-28, Opus)

**Gate = LLM'siz deterministik triyaj:** girdiyi (resolve sonucunu) güven-tabanlı
bir kovaya atar; tek cevap + hakemle AYNI DİLDE verdict
(`auto_match/review/ambiguous/no_match`). Rol netleşti (kullanıcı): 5 skorlu aday
değil, **girdiden tek çıktı + seviye**.

- **`gate/gate.py`** + CLI `inres3 gate "<sorgu>"` + `tests/unit/test_gate.py`
  (13 test). Toplam **163/163 test geçiyor**.
- **Karar OMURGASI `exact_match`** (ad/alias sorguda ARDIŞIK geçiyor mu, span≥2,
  çelişki yok, akronim değil, eşit-uzun ikinci exact yok → `auto`; iki exact →
  `ambiguous`; exact yok + tsr taban altı → `no_match`, üstü → `review`).
- **`bm25`/`kosinüs` KARARDAN ÇIKTI** (yalnız gösterim): bm25_norm sorgu-içi göreli
  (çöp de 1.0 alıp tabanı şişiriyordu), kosinüs anizotropik (§6b). Subunit seçilen
  parent'a bağlanıyor.
- **Kanıt (v2 örneklem, N=200):** ilk sürüm (bm25+marj) %79 ambiguous veriyordu
  (kullanılamaz); exact-omurga → %58 temiz auto, çöp→no_match, gerçek ikiz→ambiguous.
  Çapraz-dil'in ~1/4'ü alias-exact ile zaten çözülüyor.

**#6 marj-kapılı tsr-auto → DENENDİ ve ÇIKARILDI (tekrar deneme):** exact ıskalayan
kurtarılabilir vakalar (typo/kelime-sırası, ör. "Bozok Ünivesitesi") review'a düşüyor.
Bunları kurtarmak için tsr-auto + ayırt-edici-token kapsama kilidi yazıldı; ölçümde
iki yanlışı (locator "Calcutta" düşmesi, "Acıbadem/Adana" şehir çelişkisi)
BLOKLAYAMADI çünkü kilit `decompose.institution_part`'a dayanıyor, o dağınık
string'lerde bozuluyor ("Calcutta ... Technology" → kurum=**'Sciences'**;
"Acıbadem ... İstanbul" → kurum='Acıbadem', şehir birim'e düştü). **Asıl darboğaz
gate değil UPSTREAM: decompose.institution_part kalitesi + şehir gazetteer (ertelendi).**

**KRİTİK BULGU — gold planını etkiler:** v2 `canonical_id`, v3 ES parent id
uzayıyla **UYUŞMUYOR** (canlı doğrulandı): "Ege Ziraat" gold=114 → ES 114=ABDULLAH
GÜL; "Sakarya TÖMER" gold=180 → ES 180=İZMİR YTE; "Udmurt" gold=279 → ES'te yok.
CSV doğrudan gold DOĞRULUK için kullanılamaz (id-eşleme gerekir); geniş örneklem
için değerli.

---

## 6e. Gate iyileştirme dalgaları — Oturum 4 (2026-07-28, Opus)

Gate katmanı derinlemesine analiz edildi, yan-etki değerlendirmeli bir **dalga
planı** (P1–P8) çıkarıldı ve kanıta göre revize edildi.

**Tasarım ilkesi (blast-radius haritası):** `gate()`'i yalnız `cli/main.py`
tüketiyor (batch gate kullanmıyor, decide yok) → gate içi değişikliğin yayılımı
sadece CLI + testler. AMA `exact_match` ve `qualifier_conflict` resolve'da
hesaplanıp **judge'a da gidiyor** (`judge/prompt.py` + `judge/candidates.py`
`keep_ids` garanti-listesi). Sonuç ilke: **triyaj sezgileri gate-lokal yapılır;
resolve/normalize/judge'a DOKUNULMAZ** (kullanıcı kararı — çekirdek yol + judge
kalibrasyonu korunsun).

**Dalga 0 — ölçüm belkemiği (P8, YAPILDI):** `DENEY_2026-07-24` tablosundaki 50
sorgu sabit eval seti olarak alındı. Tablo kırpık olduğundan v2
`isimler_tekrarsız.csv`'den prefix-eşleştirmeyle tam metne çevrildi (37 tam, 13
tie→en-kısa; parent gruplarda hep aynı → parent kararı 50/50 kesin). Script'ler
scratchpad'de (`recover_50.py`, `gate_baseline.py`, `p4_experiment.py`; gold
DEĞİL, dalga etkisini ölçen dağılım/regresyon izi).

**Baseline dağılımı (N=50, LLM'siz, size=10):**

| Kova | PARENT | SUBUNIT (P2 sonrası) |
|---|---|---|
| auto_match | 30 (%60) | 16 (%32) |
| review | 17 (%34) | 24 (%48) |
| ambiguous | 2 (%4) | 1 (%2) |
| no_match | 1 (%2) | 5 (%10) |
| none | — | 4 (%8) |

Parent %60 auto, v2 N=200 "%58" ile tutarlı.

**Kanıt → plan revizyonu:** parent review'in 17/18'i `exact_yok`. Sınıflandırma:
**8 cross-lingual** (İng. sorgu / TR katalog, alias-yok) + **3 recall-miss** = 11
adet **gate'in işi DEĞİL** (upstream/veri). "tsr≥95" görünen 6 ise TUZAK (#33 MPG
şirketi, #40/#41/#48 yabancı-jenerik = doğru şekilde review'a giden yanlış-pozitif
sınıfı). **P1 morfoloji bu sette 0 vaka → ERTELENDİ** (latent hata ama bu iş yükünü
iyileştirmiyor).

**P2 — çapraz-havuz tutarlılık (YAPILDI + commit `06422b9`, gate-lokal):** Kural —
subunit `auto_match` ⇔ parent `auto_match` VE subunit o parent altında; değilse
`review`'a çekilir, `matched_id` ÖNERİ olarak korunur
(`signals.reason=parent_kesin_degil`). `_enforce_coherence` `gate.py`'de. Dalga
0'da 3 canlı ihlali kapattı: #1 (parent=ambiguous), #7 (parent=auto ama subunit
başka kurum altında, id 66 ≠ 26087), #29 (parent=review/id-yok). Parent dağılımı
değişmedi, subunit auto 19→16. 166/166 test (+3 yeni). Promosyon (güçlü subunit'in
belirsiz parent'ı netleştirmesi) bilerek dışarıda → decide/ işi.

**P4 — soft-exact: KENARDA TEST EDİLDİ, RAFA KALDIRILDI (src'ye girmedi).**
Ayırt-edici token set-kapsama, review-only. ITER1 (minimal stoplist) → 6 ateş ama
~%50 precision (alan-adı nesneleri "health science"/"dental science" sahte kapsama
üretiyor). ITER2 (alan-adı stoplist) → %100 precision AMA yalnız 2 ateş (%4), 1
doğru kaybı. RED gerekçesi: (a) temiz precision sürekli büyüyen brittle domain-liste
ister = proje felsefesine aykırı (`decompose.py` "kural yazma, veriye sor"); (b)
review-only → kova/maliyet hareketi yok; (c) asıl değer #29'daki **alias
köprüsünde** (cross-lingual), set-kapsamada değil. Kod scratchpad'de duruyor.

**Kalan gate maddeleri (hepsi düşük gözlenen değer, DURULDU):** P3 (boş/ince
havuz→review — sette tek no_match zaten gerçek çöptü), P5a (aktif qualifier
çelişkisi — bu sette karar sürüklemedi), P6 (akronim kozmetik), P7 (confidence
bağla/sil). **Asıl kaldıraç upstream:** alias-tabanlı cross-lingual recall (#29
kanıtı) — o gate değil resolve/veri işi.

---

## 7. Açık teknik borç

- **decide/ katmanı henüz YOK.** Doğrulama reddi olduğunda (JudgeValidationError)
  CLI sadece hata gösteriyor; otomatik tekrar deneme / review'e düşürme yok.
- `terminal_kaydi.txt` — çalışma sırasında oluşan kayıt, gözden geçirilip
  silinebilir (git status'ta `??`).
- **Harness hatası:** her tool çağrısında `PreToolUse/PostToolUse hook error:
  /bin/sh: node: command not found` spam'i var. Hook'lar `node` arıyor ama PATH'te
  yok. `settings.json` hook'ları düzeltilmeli (iş akışını bloklamıyor, sadece
  gürültü).

---

## 8. Sonraki oturumda ilk adım

Kod tarafı şu an: **Aşama-1 baseline + E4B + hakemden kosinüs kaldırılmış + F5
batch + Gate Aşama-1 (LLM'siz exact-omurga triyaj) + P2 çapraz-havuz tutarlılık
(commit `06422b9`).** (Aşama-2, şema-fix, Gate #6 tsr-auto ve P4 soft-exact
denenip reddedildi/rafa kaldırıldı; ayrıntı §6d/§6e.)

Açık işler (öncelik sırası):
1. **Gold değerlendirme seti** — en foundational. ⚠️ ENGEL: v2 `canonical_id`,
   v3 ES parent id'leriyle UYUŞMUYOR (§6d) → doğrudan `--gold-col` yanlış ölçer;
   önce id-eşleme ya da v3-native gold gerekir.
2. **decompose.institution_part kalitesi** — dağınık çok-cümleli girdilerde kurumu
   bozuyor (§6d); hem Gate #6'nın hem ülke/şehir çelişkisinin asıl kilidi burası.
3. **decide/ katmanı** — JudgeValidationError → otomatik tekrar/review'e düşürme.
4. **Gate ileri aşamaları** — "review"dan LLM/hakem devri; eşiklerin gold ile
   kalibrasyonu; (upstream düzelirse) #6 tsr-auto'nun geri getirilmesi.
5. İstenirse: hakemden kosinüs kalktığı için `resolve.py`'de bm25-only adaylara
   yapılan pahalı kosinüs geri-doldurması (`cosine_fn` mget yolu) atılabilir →
   hız. *Bu oturumda YAPILMADI.*
6. Düşük öncelik: MPG çapası, kuyruk hatırlatması, `judge.enabled: true`.

Deney script'leri ve ara çıktılar scratchpad'de (oturum-özel):
`.../521f4cd2-.../scratchpad` (cos_pool_rank.py, cos_hardcase*.py, cos_ablation.py,
batch_smoke.csv) ve önceki oturum `.../34c93fc2-.../scratchpad`. Kalıcı istenirse
`docs/` altına deney raporu yazılmalı.
